{% set labels = {
    'page': 'Document',
    'procedure': 'Procédure',
    'memo': 'Mémo',
    'checklist': 'Checklist',
    'referentiel': 'Référentiel',
    'naming-rule': 'Règles de nommage',
    'equipment-sheet': 'Fiche équipement'
} %}
<span class="sjdb-pill manual-type manual-type--{{ page.type|replace({'_': '-', ' ': '-'}) }}">{{ labels[page.type]|default(page.type|capitalize) }}</span>